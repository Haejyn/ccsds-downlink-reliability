# ccsds-downlink-reliability — SpaceLink

지상국 영상수신처리의 첫 단계인 **CCSDS TM 다운링크 수신 처리기**(C# / .NET 10)와 그 신뢰성 시험.
위성이 보낸 전송 프레임(CCSDS 132.0-B)을 검증하고, 가상 채널별로 Space Packet(CCSDS 133.0-B)을 재조립하며, 링크 이상을 이벤트로 보고한다.

**핵심 계약: 손상됐을 가능성이 있는 패킷은 절대 내보내지 않고, 손상과 무관한 패킷은 모두 복구한다.**
요구사항과 판정 기준: [`docs/requirements.md`](docs/requirements.md)

```
dotnet test tests/SpaceLink.Tests -c Release -p:CollectCoverage=true
```

## 결과 (2026-09-23, 로컬 .NET 10.0.401 · 뮤테이션은 CI)

| 항목 | 결과 |
|---|---|
| 시험 | **137 통과 / 0 실패** (xUnit) |
| 뮤테이션을 심판으로 세운 시험 생성 루프 | `tools/mutation_loop/` — 생존 뮤턴트를 모델에 넘겨 시험을 쓰게 하고, **생존이 줄 때만 채택**한다. `ReedSolomon.cs` 에서 생존 10 → 7, 죽은 셋은 전부 REQ-RS-03 의 핵심([시험 보고서 §10](docs/test-report.md)) |
| 커버리지 (coverlet) | 라인 **100 %** (786/786) · 분기 **99.7 %** (329/330 — x64 에서 갈 수 없는 SSSE3 없음 가지) · 메서드 100 % |
| 채널 부호 (RS·ASM·PN) | RS(255,223) 심볼 오류 **16 개까지 정정**, 17 개 이상은 3,000/3,000 **실패 선언**(오정정 0). 3 비트 슬립에서 10 장 중 7 장 복원 후 재동기 |
| 뮤테이션 검출률 (Stryker.NET) | **95.38 %** (736 개 — 죽임 626 · 타임아웃 76 · 생존 34, 실제 캡처 뒤 [CI run](https://github.com/Haejyn/ccsds-downlink-reliability/actions/runs/35838269054)) — 그 전 94.97 %(736 개)·95.49 %(710 개). 같은 코드도 실행마다 약 ±0.4 %p 흔들려(죽임·타임아웃·생존 경계가 러너 속도에 따라 갈린다) 한 번의 점수로 읽지 않는다. 생존은 메시지 문자열·동등·판정된 성능 정책이고, 남은 실제 약점은 하나다 ([시험 보고서 §9.5·§9.8](docs/test-report.md)) |
| 요구사항 추적 | **30 / 30** ([`docs/traceability.md`](docs/traceability.md), `python tools/trace.py` 가 생성) |
| 빌드 | 컴파일러·.NET 분석기(latest-recommended) 경고를 오류로 — 경고 0 |
| 처리량 — **추출 단계만** (BenchmarkDotNet, 단일 스레드) | **2,785,000 프레임/초** ≈ **2.85 Gbps** (128 바이트 프레임 78,089 장 / 28.04 ms) · PEC 검증 포함 1,611,000 프레임/초. 프레임을 패킷으로 조립하는 **마지막 단계**의 값이다 |
| 처리량 — **수신 체인 전체** (ASM → PN → RS → 추출) | 단일 스레드 **약 1,025,000 프레임/초 ≈ 채널 1.34 Gbps**(CADU 164 B) — v1.0 의 54,800 에서 **18.7 배**. RS 복호만 코어에 나누면 **약 1,460,000 프레임/초 ≈ 채널 1.9 Gbps**(2 Gbps 의 96 %). 부호어당 심볼 오류 8 개를 정정하면 단일 150,000 · 병렬 546,000 프레임/초. 단계별로 재 보니 시간의 **90 % 가 RS 신드롬**이라 SIMD(PSHUFB 표 곱셈)로 바꿨다 ([§8.6](docs/test-report.md), 같은 조건 전후 비교, short job) |
| 프레임당 할당 | 추출 **243 B** (개선 전 약 443 B) · PEC 경로 **242 B** · 체인 **810 B**(짧은 코드블록 전 1,247) · 정정 경로 **1,066 B**(전 2,007 · 2,443) · 병렬 827 · 1,085 B — CI 가 벤치마크별 예산으로 막는다 |
| 발견·수정 결함 | **C-1** 프레임 카운트로 감지되지 않는 유실에서 손상 패킷 출력 · **CH-4** PN 수열이 표준과 달랐다 — 주기·역원 시험은 통과했고 표준 문서의 값과 대조하고서야 드러났다 ([시험 보고서 §9.6](docs/test-report.md)) · **CH-5** 짧은 프레임을 뒤에 0 을 채워 255 바이트 전부 보냈다 — 표준은 앞쪽 가상 채움을 보내지 않는다, 패리티도 달랐다 ([§9.8](docs/test-report.md)) |

→ [시험 보고서](docs/test-report.md) · [시험 계획서](docs/test-plan.md) · [요구사항](docs/requirements.md) · [추적 매트릭스](docs/traceability.md)

## 시험 구성

| 영역 | 무엇을 | 규모 |
|---|---|---|
| CRC | 표준 검사값(0x29B1) · 표 기반 구현 = 비트 단위 기준 구현 | 무작위 버퍼 3,000 |
| 프레임 오류 검출 | 1115 바이트 프레임 **단일 비트 오류 전수(8,920 가지)** · 2/3/5 비트 무작위 · 16 비트 이하 버스트 | 각 20,000 |
| 비트 배치 | 프레임·패킷 주 헤더를 수작업 비트 벡터와 대조, 무작위 왕복 | 프레임 4,000 · 패킷 5,000 |
| 구조 검증 | CRC 가 맞아도 버전·동기 플래그·세그먼트 길이·제1 헤더 포인터 범위·OCF 플래그 오류는 사유와 함께 거부 | — |
| 정상 수신 | 여러 프레임에 걸친 패킷, 프레임 경계에서 쪼개진 헤더, 가상 채널 교차, 유휴 프레임, 8 비트 카운트 순환 | 패킷 3,000 |
| 결함 주입 | 프레임 유실(2·5·20 %) · 비트 오류 · 중복 · 순서 뒤바뀜 — 출력 = "손상 프레임과 겹치지 않는 원본 패킷 전부" 와 정확히 일치 | 패킷 2,000씩 |
| 감지 불가 유실 | 256·512 장 연속 유실 (카운트가 한 바퀴 돌아 연속으로 보임) × 40 시도 × 5 시드 | — |
| 강건성 | 무작위 바이트 50,000 · CRC 가 맞는 무작위 프레임 50,000 — 예외 0 | — |
| RS 정정 | 심볼 오류 0…16 정정 · 17 이상 실패 선언(오정정 0) · 인터리빙 버스트 · 체 이론으로 구성한 실패 경로 · 짧은 코드블록(가상 채움) · 인터리빙 깊이 1·2·3·4·5·8 만 | t = 0…16 × 시드 5 · 3,000 회 |
| 표준·외부 대조 | 부속서 G 계수표 · 부속서 F 행렬 · PN 처음 40 비트(255·131071) · libfec 기준 벡터 | 부호화 35 · 복호 25 |
| 프레임 동기 | 비트 단위 ASM 탐색 · 비트 슬립 재동기 · flywheel · 버퍼 유계(REQ-ASM-04) | CADU 4,000 |
| 실제 위성 캡처 | Astrocast 0.1 9k6 녹음을 복조한 비트열 → 프레임 CRC 까지, 실제 코드블록 위 오류 주입 | CADU 3 |

## 발견한 결함 C-1

**증상** — 256 장 단위로 프레임이 연속 유실되면 8 비트 가상 채널 프레임 카운트가 한 바퀴 돌아 유실이 보이지 않는다. 이때 조립 중이던 패킷에 전혀 다른 패킷의 뒷부분이 이어 붙어, **첫 설계는 원본에 없는 손상 패킷을 내보냈다** (시험 5건 실패).

**원인** — 프레임 CRC·프레임 카운트·제1 헤더 포인터는 모두 *프레임 단위* 정보다. 우연히 경계가 맞으면 프레임 계층만으로는 잘못 이어 붙인 패킷을 원리적으로 구분할 수 없다.

**조치** — 패킷 오류 제어(PEC, 패킷 끝 CRC-16)를 선택 기능으로 넣고, 수신 쪽이 검증해 불일치하면 버리고 다음 제1 헤더 포인터에서 다시 동기화한다. 비용은 패킷당 2 바이트.

**고정한 시험** — (1) PEC 없이는 손상 패킷이 실제로 나온다(결함이 조건에서 재현됨을 고정) (2) PEC 를 켜면 같은 조건 5 시드 × 40 시도에서 손상 패킷 0 (3) PEC 는 패킷 단일 비트 오류 전수를 잡는다.

## 한계와 다음

- **처리량 수치를 시험 안에서 재지 않는다** — 예전 README 의 "약 3만 프레임/초" 는 xUnit 이 시험을 병렬로 돌리는 동안 잰 값이라 수신기 성능이 아니라 CPU 경합을 재고 있었다. 단독 측정은 20~25배 빠르다. 지금 수치는 전부 `benchmarks/SpaceLink.Benchmarks` (단일 스레드) 에서 나온다
- **2.85 Gbps 는 추출 단계만의 값이었다** — 체인 전체는 한때 약 55,000 프레임/초로 **추출 단계의 1/53** 이었다(§8.5). 단계별로 재서 RS 신드롬이 90 % 인 것을 확인하고 고쳐 지금은 단일 스레드 1.34 Gbps · 병렬 약 1.9 Gbps 다(§8.6). **이제 순서대로 한 줄로 도는 패킷 추출(약 347 ns/프레임)이 병렬 체인의 한계**다 — 가상 채널별로 나누면 더 갈 수 있지만 하지 않았다. 오류가 많은 링크(부호어당 8 개)에서는 병렬도 약 0.7 Gbps 다
- **남은 할당** — 프레임당 243 B 의 대부분은 `SpacePacket` 이 패킷마다 자기 데이터 배열을 갖기 때문이다. 더 줄이려면 호출자가 버퍼를 넘기거나 풀을 쓰는 형태로 **공개 API 를 바꿔야 한다** — 지금은 쉬운 API 를 택했다
- **RS 는 이제 CCSDS 의 코드다** — 생성 다항식의 근을 표준(§4.3.4, `(α^11)^j, j = 112…143`)대로 고치고 이중 기저(dual basis, §4.3.9, 부속서 F) 변환을 넣었다. 부속서 G 가 싣는 생성 다항식 계수 32 개와 값으로 대조한다 ([시험 보고서 §9.7](docs/test-report.md)). PN 수열은 표준의 기본인 131071 비트와 옛 호환용 255 비트 둘 다 있고, 둘 다 표준이 싣는 처음 40 비트와 대조한다([§9.9](docs/test-report.md)). **부호화·복호를 공개 구현 libfec 의 실제 출력과도 바이트 단위로 대조한다**([§9.10](docs/test-report.md)). **실제 위성(Astrocast 0.1)이 보낸 전파의 녹음을 복조해 ASM → PN → RS(인터리빙 5) → 프레임 CRC 까지 복호한다**([§9.11](docs/test-report.md)) — 깨끗한 짧은 녹음이라 **잡음이 섞인 실제 패스도 복호한다**: EIRSAT-1 을 SatNOGS 지상국이 녹음한 188 초에서 CADU 46 장 중 31 장을 **심볼 323 개를 정정해** 복원했고, 같은 녹음을 그 지상국이 복호한 26 장 중 25 장과 바이트 단위로 같다. 그 지상국이 놓친 6 장도 복원했는데 프레임 카운트의 빈자리에 정확히 들어가고 패킷 88 개가 그 프레임들을 가로질러 끊김 없이 조립된다([§9.13](docs/test-report.md))
- 컨볼루션·터보·LDPC 부호, AOS 프레임, 패킷 부헤더(시각 코드)는 범위 밖
- 이벤트·예외의 **메시지 문구는 계약이 아니라고 보고 시험하지 않는다** — 생존 뮤턴트 대부분이 이 문자열이라, 문구를 계약으로 삼지 않는 한 점수에 상한이 있다 ([시험 보고서 §4](docs/test-report.md))
